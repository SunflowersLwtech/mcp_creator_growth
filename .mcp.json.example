{
  "mcpServers": {
    "mcp-creator-growth": {
      "type": "stdio",
      "command": "${HOME}/mcp-creator-growth/.venv/bin/python",
      "args": ["-m", "mcp_creator_growth"],
      "env": {
        "MCP_DEBUG": "${MCP_DEBUG:-false}"
      }
    }
  }
}
